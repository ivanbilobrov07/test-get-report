import styled from "styled-components";
import { Box } from "~/shared";

export const PaymentStatusdBox = styled(Box)`
  display: flex;
  flex-direction: column;
`;

export const PaymentStatusText = styled.p`
  color: rgb(155, 143, 143);
`;
